name: DT Day After

on:
  schedule:
    - cron: "8,23,38,53 * * * *"
  workflow_dispatch:
    inputs:
      target_game_id:
        description: "Optional game_id to target"
        required: false
        type: string
      force_demo:
        description: "Bypass normal final gating"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: dt-dayafter
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: python dt_dayafter.py
        env:
          GHOST_URL: ${{ secrets.GHOST_URL }}
          GHOST_ADMIN_KEY: ${{ secrets.GHOST_ADMIN_KEY }}
          DT_TARGET_GAME_ID: ${{ inputs.target_game_id }}
          DT_FORCE_DEMO: ${{ inputs.force_demo }}
      - run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add content_state.json season_state.json
          git diff --cached --quiet && echo "No dayafter state changes to commit" || git commit -m "Update day after state"
          git diff --cached --quiet && exit 0
          git pull --rebase origin main
          git push
